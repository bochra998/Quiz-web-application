# -*- coding: UTF-8 -*-

import django.conf.urls


urlpatterns = patterns('',
                       django.conf.urls.url(r"^/?", views.PingView.as_view(), name="django-session-idle-timeout_ping"),
                       )
